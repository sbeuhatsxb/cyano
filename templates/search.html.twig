{% extends 'homepage.html.twig' %}

{% block javascripts %}
    {{ encore_entry_script_tags('matchHeight') }}
    {{ encore_entry_script_tags('js/search') }}
{% endblock %}

{% block body %}
    <div class="jumbotron-fluid d-flex">
    <div class="container" style="min-height: 500px">

        <div class="container">
            <div class="row">
                <form action="{{ path('searchbar') }}" method="get">
                    <div class="container" style="margin-bottom: 20px">
                        <div class="row">
                            <input name="q"
                                   class="form-control search-field"
                                   placeholder="Recherche"
                                   autocomplete="off"
                                   autofocus
                                   data-no-results-message="Pas de résultat"
                            >
                        </div>
                    </div>
                </form>
            </div>
        </div>
        <div id="results"></div>
        {#MAIN ARTICLE END#}

    </div>
    {#SECOND CONTAINER END#}

    </div>
    {#END BLOCK BODY#}
{% endblock %}
